FsLex文本符号化/词法分析

文件扩展名 .fsl
文件有一个可选的头，放在大括号 {}
规则(rule)是正则表达式的集合
规则的定义：
使用关键字 rule + 规则名 + 等号 + 关键字 parse
正则表达式定义：
加一个动作，用大括号括起来的 F# 表达式
每个规则用 | 隔开
每个规则根据文本流的匹配情况，将变成一个函数
若有几个正则表达式均匹配此规则，采用最长匹配
未找到匹配则抛出异常
一个词法分析器可以包含几个规则，多个规则间用 and 分割

